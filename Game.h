#pragma once

#include "Player.h"
#include <vector>

class Game {
    public:
        Game();
        int run();

    private:
        // Player player;
        // int lives;
        // int score;

        // void InitGame();
        // void UpdateGame();
        // void DrawGame();
        // void UnloadGame();
};